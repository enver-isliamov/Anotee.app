
# 💳 План внедрения подписок через ЮKassa (Recurrent)

Этот документ описывает процесс подключения подписок (Free/Pro) с автосписанием, используя **ЮKassa + Clerk + Vercel Cron**.

---

## 🏗 Архитектура

1.  **База данных:** Вся информация о подписке хранится в **Clerk Public Metadata** пользователя:
    *   `plan`: 'free' | 'pro'
    *   `expiresAt`: Timestamp (дата окончания доступа).
    *   `yookassaPaymentMethodId`: ID сохраненной карты для автосписания.
2.  **Первый платеж:** Пользователь платит вручную, мы передаем флаг `save_payment_method: true`.
3.  **Обработка:** Вебхук ЮKassa подтверждает оплату -> мы продлеваем `expiresAt` и сохраняем ID карты.
4.  **Авто-продление:** Vercel Cron (бесплатно) раз в день запускает скрипт, который находит пользователей с истекающим сроком и делает запрос в ЮKassa на списание денег по сохраненной карте.

---

## 🛑 ЭТАП 1: ПОДГОТОВКА (ВАШИ ДЕЙСТВИЯ) 🛑

Эти действия необходимо выполнить в личном кабинете ЮKassa **до** написания кода.

1.  **Регистрация и Договор:**
    *   Зарегистрируйтесь на [yookassa.ru](https://yookassa.ru/).
    *   Для тестов можно не подписывать договор сразу, а использовать **Тестовый магазин**.

2.  **Получение ключей (Test Mode):**
    *   В ЛК перейдите в **Интеграция** -> **Ключи API**.
    *   Вам нужен **Shop ID** (идентификатор магазина).
    *   Вам нужен **Secret Key** (Секретный ключ).
    *   *Сохраните их в блокнот.*

3.  **Настройка HTTP-уведомлений (Webhooks):**
    *   В разделе **Интеграция** -> **HTTP-уведомления**.
    *   URL для уведомлений: `https://ващ-домен.vercel.app/api/webhooks/yookassa` (пока нет домена, можно использовать ngrok или пропустить этот шаг, настроив позже).
    *   Отметьте события: `payment.succeeded`, `payment.canceled`.

4.  **Активация автоплатежей (Важно!):**
    *   Напишите в поддержку или проверьте в настройках, что у вас включены **"Рекуррентные платежи" (Безакцептные списания)**. Без этого автопродление не сработает.

---

## ⚙️ Этап 2: Настройка окружения

Добавьте переменные в файл `.env` (и в Vercel Settings):

```env
# YooKassa Credentials
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=test_... (начинается на test_ или live_)

# App URL (для редиректов после оплаты)
NEXT_PUBLIC_APP_URL=https://anotee.com

# Cron Secret (для защиты скрипта автосписания)
CRON_SECRET=придумайте_длинный_пароль
```

Установите библиотеку (опционально, можно и просто через fetch, но sdk удобнее):
```bash
npm install yookassa-ts uuid
```

---

## 💻 Этап 3: Бэкенд (API Routes)

Нам нужно 3 эндпоинта.

### 3.1. Создание платежа (`api/yookassa/init.js`)
Этот роут создает ссылку на оплату.

**Логика:**
1.  Получить `userId` из сессии.
2.  Отправить запрос в ЮKassa (`POST /payments`):
    *   `amount`: { value: '1500.00', currency: 'RUB' }
    *   `payment_method_data`: { type: 'bank_card' }
    *   `confirmation`: { type: 'redirect', return_url: '...' }
    *   `save_payment_method`: **true** (Критично для подписок!)
    *   `metadata`: { userId: 'user_123...' }
3.  Вернуть фронтенду `confirmation.confirmation_url`.

### 3.2. Вебхук (`api/webhooks/yookassa.js`)
Слушает ответ от ЮKassa.

**Логика:**
1.  Проверить IP (опционально) или просто принять JSON.
2.  Если событие `payment.succeeded`:
    *   Достать `userId` из `object.metadata`.
    *   Достать `payment_method.id` из `object.payment_method`.
    *   **Действие:** Обновить Clerk Metadata пользователя:
        ```json
        {
          "plan": "pro",
          "yookassaPaymentMethodId": "2b9...",
          "expiresAt": 1745678900000 // Текущая дата + 30 дней
        }
        ```
    *   Ответить ЮKassa `200 OK`.

### 3.3. Автопродление (`api/cron/renew.js`)
Скрипт, который будет дергать Vercel Cron раз в сутки.

**Логика:**
1.  Проверить заголовок `Authorization: Bearer CRON_SECRET`.
2.  Получить из Clerk список пользователей, у которых `plan: 'pro'` и `expiresAt` < (Завтра).
3.  Для каждого такого пользователя создать платеж в ЮKassa:
    *   Вместо `confirmation: redirect` передать `payment_method_id` (который мы сохранили в шаге 3.2).
    *   ЮKassa спишет деньги без участия юзера.
4.  Если списание успешно -> Продлить `expiresAt` еще на 30 дней.
5.  Если ошибка (нет денег) -> Сбросить `plan` на `free`.

---

## 🖥 Этап 4: Фронтенд (UI)

### 4.1. Хук статуса (`hooks/useSubscription.ts`)
```typescript
export const useSubscription = () => {
  const { user } = useUser();
  const meta = user?.publicMetadata;
  
  // Проверяем не просто флаг, а дату!
  const isPro = meta?.plan === 'pro' && meta?.expiresAt > Date.now();
  
  return { isPro, expiresAt: meta?.expiresAt };
};
```

### 4.2. Интеграция в плитки цен (`components/RoadmapBlock.tsx`)
Вместо создания отдельной страницы оплаты, мы модифицируем существующие карточки тарифов.

1.  **Модификация компонента `RoadmapBlock`:**
    *   Добавить состояние `isBuying` (loading state).
    *   Добавить функцию `handleBuy`:
        *   Вызывает `/api/yookassa/init`.
        *   Получает `confirmation_url`.
        *   Перенаправляет пользователя: `window.location.href = url`.
2.  **Изменение карточки "Ранний доступ":**
    *   Найти блок с текстом "Статус: Открыто" (внизу карточки).
    *   Заменить его на кнопку `<button onClick={handleBuy}>Купить за 2900₽</button>`.
    *   Стилизовать кнопку (зеленая/индиго, с эффектом нажатия).
3.  **Обработка остальных карточек:**
    *   У карточек Этап 2 и 3 кнопки должны быть `disabled` с текстом "Скоро" или иконкой замка.

---

## 🛑 ЭТАП 5: НАСТРОЙКА CRON (ВАШИ ДЕЙСТВИЯ) 🛑

Чтобы автопродление работало, нужно настроить файл `vercel.json` в корне проекта.

1.  Создайте или обновите `vercel.json`:
```json
{
  "crons": [
    {
      "path": "/api/cron/renew",
      "schedule": "0 10 * * *" 
    }
  ]
}
```
*(Это означает: запускать скрипт каждый день в 10:00 утра)*.

---

## ✅ Чек-лист проверки

- [ ] 🛑 Вы создали магазин в ЮKassa и получили `shopId`.
- [ ] 🛑 Вы добавили переменные окружения в `.env`.
- [ ] Пользователь нажимает "Купить" в карточке тарифа, переходит на сайт ЮKassa.
- [ ] Оплачивает (тестовой картой).
- [ ] В дашборде Clerk у пользователя обновилась metadata (`plan: pro`).
- [ ] Интерфейс Anotee разблокировал Pro-функции.
- [ ] (Сложный тест) Вручную уменьшить дату `expiresAt` в Clerk и запустить `/api/cron/renew` -> должно произойти повторное списание.


# 🚜 SmoTree Refactoring Roadmap

## 📋 План Действий

## 🚀 Этап VII: SaaS Архитектура (Contextual Permissions)
**Статус:** ✅ **Этап завершен.**

## 🚑 Этап VIII: Стабилизация и Очистка (Security Cleanup)
**Статус:** ✅ **Этап завершен.**

## 🛑 Этап IX: Critical Refactoring & Bug Fixes (Завершен)
**Статус:** ✅ **Этап завершен.**

---

## 💀 Этап X: Technical Debt & Critical Fixes (Текущий)
**Статус:** 🚧 **В работе**

Эти проблемы блокируют нормальную работу после миграции на Clerk.

### 1. Исправление Vercel Blob Upload (Broken Auth)
- [ ] **Проблема:** `useUploadManager` ищет токен в `localStorage` (ключ `smotree_auth_token`), который мы удалили. Загрузка падает.
- [ ] **Проблема:** `api/upload.js` ждет заголовок `Authorization`, но клиентский SDK `@vercel/blob` не умеет его слать в запросе на генерацию токена.
- [ ] **План:**
    - Обновить `useUploadManager`: принимать `getToken` из Clerk.
    - Передавать токен в `clientPayload` или через Query Params в `handleUploadUrl`.
    - Обновить `api/upload.js` для чтения токена из альтернативного источника (если хедера нет).

### 2. Починка доступа к Personal Projects
- [ ] **Проблема:** `api/data.js` (GET) выбирает проекты только по `owner_id` или `org_id`.
- [ ] **Баг:** Если добавить пользователя в `team` (через кнопку Invite в личном проекте), он все равно не увидит проект, так как SQL запрос игнорирует поле `team` внутри JSON.
- [ ] **План:**
    - Обновить SQL запрос в `api/data.js`: Добавить условие `OR projects.data->'team' @> '[{"id": "USER_ID"}]'`.
    - Это вернет обратную совместимость для личного шеринга.

### 3. Очистка Player.tsx
- [ ] **Проблема:** Метод `persistLocalFile` неявно добавляет юзера в массив `team`. Это "Side Effect", который может привести к багам доступа.
- [ ] **План:** Убрать мутацию `team` из функции линковки локального файла.

### 4. Очистка App.tsx и Hooks
- [ ] **Проблема:** Передача `lastLocalUpdateRef` и сложная логика блокировки поллинга размазана по компонентам.
- [ ] **План:** Централизовать логику синхронизации, убрать чтение `localStorage` для аутентификации.

### 5. UI Полировка
- [ ] **Проблема:** Кнопка "Invite" в личном проекте выглядит так же, как в Организации, но работает через legacy-механизм.
- [ ] **План:** Добавить визуальный индикатор или тултип, что это "Personal Share" (доступ только к этому файлу/проекту), в отличие от "Team Member" (доступ ко всей организации).

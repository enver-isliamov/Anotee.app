
# 🚜 SmoTree Refactoring Roadmap

## 📋 План Действий

## 🚀 Этап VII: SaaS Архитектура (Contextual Permissions)
**Статус:** ✅ **Этап завершен.**

## 🚑 Этап VIII: Стабилизация и Очистка (Security Cleanup)
**Статус:** ✅ **Этап завершен.**

## 🛑 Этап IX: Critical Refactoring & Bug Fixes (Завершен)
**Статус:** ✅ **Этап завершен.**

## 💀 Этап X: Technical Debt & Critical Fixes (Завершен)
**Статус:** ✅ **Этап завершен.**

## 🧹 Этап XI: Финализация и исправление Архитектурных Дыр
**Статус:** ✅ **Этап завершен.**

---

## 🛡️ Этап XII: Безопасность и Оптимизация (Текущий)
**Статус:** ✅ **Завершен**

Устранение критических уязвимостей и архитектурных "мин замедленного действия", выявленных при аудите.

### 1. 🔥 Critical Security: SSRF в Proxy Audio
- [x] **Проблема:** `api/proxyAudio.js` валидирует URL простой проверкой подстроки `.includes('drive.google.com')`. Это уязвимо для SSRF атак (например, через поддомены или перенаправления).
- [x] **Решение:** Внедрена строгая Regex-валидация для извлечения Google Drive File ID. URL конструируется на сервере.

### 2. 💣 Timebomb: Out-Of-Memory в Миграции
- [x] **Проблема:** `api/migrate.js` делает `SELECT *` без `LIMIT`. При росте БД скрипт упадет по памяти (Lambda OOM).
- [x] **Решение:** Реализовано Batch-процессинг (LIMIT 50 OFFSET X) с циклом.

### 3. 📉 Performance: "Тяжелый" Поллинг
- [x] **Проблема:** `App.tsx` запрашивает полный JSON всех проектов каждые 10 секунд. Это убивает лимиты Vercel Postgres и трафик.
- [x] **Решение:** 
    - Создан эндпоинт `/api/check-updates`, возвращающий только timestamp.
    - Клиент проверяет timestamp перед загрузкой тяжелого JSON.

### 4. 🧩 Inconsistency: Унификация User Object
- [x] **Проблема:** `api/upload.js` создает user-объект вручную.
- [x] **Решение:** Логика вынесена в `api/_auth.js` (`getUserFromToken`) и переиспользована везде.

### 5. 🏗️ Refactor: Удаление Event Bus "Костылей"
- [x] **Проблема:** Статус подключения Google Drive обновлялся через `window.dispatchEvent`.
- [x] **Решение:** Реализован `DriveContext` (React Context) для управления состоянием подключения.

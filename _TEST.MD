
# 🧪 Anotee Testing Documentation

Этот документ описывает стандарты, стратегии и чек-листы для тестирования проекта Anotee.
В проекте используется комбинированный подход: автоматизированные тесты для логики и API, плюс строгое ручное/скриптовое тестирование для видео-плеера и интеграций.

---

## 1. Стек и Инструменты

*   **Unit Testing:** `TestRunner.tsx` (In-App System Diagnostics).
*   **API Testing:** `Postman` или `Hoppscotch` (для локальной отладки Serverless функций).
*   **Environment:** Тесты запускаются прямо в production-сборке через секретный маршрут `/test` или кнопку "System Tests" в Админ панели.

---

## 2. In-App UI Testing (`TestRunner.tsx`)
Доступен по кнопке "System Tests" в Админ панели. Выполняет быструю проверку критической бизнес-логики без запуска бэкенда.

### 🧪 Реализованные тесты

#### 1. Core Math & Timecodes
*   **FPS Math (25fps PAL):** Проверка точности расчета кадров. Гарантирует отсутствие "дрифта" звука при конвертации секунд в кадры.
*   **FPS Math (29.97fps NTSC):** Проверка обработки дробных FPS.

#### 2. Time & Logic
*   **SMPTE Zero Check:** Проверка форматирования `00:00:00:00`.
*   **SMPTE Complex Check:** Проверка форматирования таймкода со смешанным временем (минуты, секунды, кадры).
*   **Expiry Check (>7 Days):** Проверка логики истечения срока проекта (Free Tier Limits).
*   **Days Remaining:** Проверка расчета оставшихся дней.

#### 3. Export Generators
*   **EDL Structure:** Валидация заголовков экспортируемого файла.
*   **XML Color Mapping:** Проверка, что статус комментария (`OPEN`/`RESOLVED`) корректно мапится в цвета маркеров Resolve (Red/Green).
*   **CSV Sanitization:** Проверка экранирования кавычек (Excel CSV injection prevention).
*   **XML Entity Escaping:** Проверка экранирования спецсимволов (`&`, `<`, `>`) во избежание генерации битых XML файлов.

#### 4. System Utils
*   **ID Generator Collision:** Генерация 100 UUID и проверка на уникальность.
*   **ID Format Check:** Валидация формата ID (UUID v4 structure).
*   **Color Determinism:** Проверка, что функция `stringToColor` всегда возвращает одинаковый цвет для одного и того же User ID (нет случайности).

#### 5. Data Integrity & Configs (New)
*   **Default Config Integrity:** Проверка наличия критических флагов (`google_drive`, `max_projects`) в случае сбоя загрузки с сервера.
*   **Payment Defaults:** Валидация структуры тарифных планов (наличие Free и Lifetime, цены > 0).
*   **Mock Data Validity:** Проверка целостности демо-данных (наличие ассетов и версий).

---

## 3. Интеграционное Тестирование (Backend API)
*Цель: Проверка Serverless функций в изоляции.*

### 🔐 Permissions & ACL (`api/_permissions.js`)
Самая важная часть безопасности.
*   **Сценарий 1 (Owner):** Может редактировать, удалять, менять настройки.
*   **Сценарий 2 (Team Member):** Может комментировать, не может удалять проект.
*   **Сценарий 3 (Restricted Guest):**
    *   Запрос `/api/data?projectId=X&assetId=Y`.
    *   *Expect:* В ответе массив `assets` содержит ТОЛЬКО 1 элемент. Массив `team` пустой или отфильтрован.
*   **Сценарий 4 (Public Access):**
    *   Если `publicAccess: 'none'` -> *Expect:* 403 Forbidden.
    *   Если `publicAccess: 'view'` -> *Expect:* 200 OK (Read Only).

### ☁️ Storage API (`api/storage.js`)
*   **Presigning:** Проверка генерации ссылок AWS S3 (наличие `Signature` в query params).
*   **Secret Decryption:** Проверка, что ключи S3 корректно расшифровываются из БД.

### 💳 Payments (`api/payment.js`)
*   **Webhook Handling:**
    *   Симуляция `payment.succeeded` от ЮKassa.
    *   Проверка идемпотентности (повторный вебхук не должен продлевать подписку дважды).
    *   Проверка начисления правильного тарифа (`lifetime` vs `pro`).

---

## 4. E2E Тестирование (End-to-End)
*Цель: Проверка пользовательских путей (Happy Path).*

### Сценарий "Onboarding & Upload"
1.  Регистрация нового юзера (Mock Clerk).
2.  Создание проекта "Demo Project".
3.  Загрузка файла `.mp4` (Mock Upload).
4.  Проверка появления файла в списке ассетов.

### Сценарий "Review Workflow"
1.  Открытие плеера.
2.  Клик по таймлайну -> Постановка комментария.
3.  Ввод текста -> Отправка.
4.  Перезагрузка страницы -> Комментарий сохранился и отображается на таймлайне.

### Сценарий "Sharing"
1.  Владелец включает "Public Access".
2.  Копирует ссылку.
3.  Открывает ссылку в инкогнито (без авторизации).
4.  *Expect:* Проект открывается, видео играет, кнопки "Delete" скрыты.

---

## 5. Медиа и Стриминг QA (Специфично для Anotee)
*Эти тесты часто требуют ручной проверки или специализированных скриптов.*

### 🎥 Playback Reliability (Железные правила)
*   **Google Drive Link:** Проверить, что URL имеет вид `drive.google.com/uc?export=download`.
*   **CORS Attribute:** Проверить через DevTools, что у тега `<video>` **НЕТ** атрибута `crossOrigin="anonymous"` при воспроизведении с Drive. Если он есть — тест провален.
*   **Range Requests:**
    *   В DevTools Network проверить запрос видео.
    *   Статус ответа должен быть `206 Partial Content`.
    *   Если `200 OK` (весь файл) -> Тест провален (скраббинг не будет работать).

### ☁️ S3 (BYOS) Integration
*   **CORS Config:** Проверить, что заголовки `Access-Control-Allow-Origin` возвращаются с бакета пользователя.
*   **Mime Types:** Проверить, что видео отдает правильный `Content-Type: video/mp4`, иначе Safari не воспроизведет.

---

## 6. Ручное Тестирование (UX Checklist)

Перед каждым релизом проверять:

*   **Mobile Experience:**
    *   [ ] Свайп влево/вправо по комментарию в списке (Delete/Edit).
    *   [ ] Плеер не перекрывается клавиатурой при вводе комментария.
    *   [ ] Гамбургер-меню открывается и закрывается.
*   **Performance:**
    *   [ ] Скраббинг видео мышкой ("Dead Zone" check). Видео не должно дергаться при микро-движениях.
    *   [ ] Переключение версий (v1 -> v2) происходит без перезагрузки страницы.
*   **Optimistic UI:**
    *   [ ] При смене названия проекта UI обновляется мгновенно, не дожидаясь ответа сервера.

---

## 7. Тестирование Безопасности (Security)

*   **Rate Limiting:** Проверка защиты от DDOS на эндпоинтах `/api/upload` и `/api/data`.
*   **Data Isolation:** Убедиться, что запрос списка проектов возвращает только проекты текущего `ownerId` или `orgId`.
*   **Token Validation:** Попытка доступа к API с истекшим или невалидным JWT токеном должна возвращать `401`.

---

## 8. Локализация и Кириллица (I18N)

Anotee используется в РФ, поэтому поддержка кириллицы критична.

*   **Sanitization Check:**
    *   [ ] Загрузить файл `Тестовое Видео.mp4`.
    *   [ ] Проверить, что название в дашборде не сбросилось в дефолтное `Video_Asset` или пустую строку (Regex должен пропускать Unicode).
*   **Export Encoding:**
    *   [ ] Создать комментарий "Сцена 1: Исправить цвет".
    *   [ ] Экспортировать в CSV.
    *   [ ] Открыть в Excel. Убедиться, что кодировка распознана (Excel требует BOM `\uFEFF` для UTF-8).
*   **URL Handling:**
    *   [ ] Проверить генерацию ссылок для S3 объектов с кириллицей в ключе (`anotee/project/файл.mp4`).
    *   [ ] Убедиться, что ссылка воспроизводится в плеере (корректный `encodeURIComponent`).


# 🧪 Anotee Testing Documentation v1.2602.1

В версии 1.2602.1 мы расширили покрытие тестами, добавив проверки авторизации, плеера, хранилища и биллинга.

---

## 1. Архитектура

*   **UI Компонент:** `components/TestRunner.tsx` — Отвечает только за рендер дашборда, логи и запуск.
*   **Логика:** `services/testSuite.ts` — Содержит массив `TEST_SUITE` с реализацией всех проверок.

## 2. Категории Тестов (`services/testSuite.ts`)

### 🌐 Backend API Integrity (`api`)
*   **API Healthcheck:** Пинг эндпоинта `/api/health`. Ожидает `200 OK`.
*   **Auth Guard:** Пинг защищенного `/api/data` без токена. Ожидает `401 Unauthorized`. *Это критический тест безопасности.*

### 🔐 Auth & Permissions (`auth`)
*   **Org Admin Detection:** Проверяет утилиту `isOrgAdmin`, убеждаясь, что роль `org:admin` корректно распознается в списке участников.

### 🎥 Media Streaming QA (`media`)
*   **Video CDN Access:** HEAD-запрос к публичному видео-файлу Google Storage. Проверяет доступность сети, CORS заголовки и MIME-тип `video/mp4`.

### 🎬 Player Core (`player`)
*   **Deterministic Color Hash:** Проверяет, что функция генерации цвета для аватарки (`stringToColor`) возвращает одинаковый HSL-цвет для одного и того же User ID.

### ☁️ Storage Logic (`storage`)
*   **Upload Security Guard:** Симуляция проверки типа файла. Убеждается, что система блокирует загрузку небезопасных форматов (например, `.exe`).

### 💳 Billing & Limits (`billing`)
*   **Subscription Expiry Check:** Проверяет логику истечения срока подписки (функция `isExpired`).
*   **Days Remaining Calc:** Проверяет точность расчета оставшихся дней пробного периода.

### ⚙️ System Utils (`sys`)
*   **UUID Generation:** Проверяет формат и длину генерируемых уникальных идентификаторов.

### 🧮 Core Logic & Math (`math`)
*   **FPS Calculation:** Проверка точности расчета кадров (во избежание дрифта звука).
*   **SMPTE Timecode:** Проверка форматирования `65.5с` -> `00:01:05:12`.

### 📤 Export Engines (`export`)
*   **EDL Header:** Валидация структуры EDL файлов для монтажа.
*   **XML Escaping:** Проверка защиты от битых XML при использовании спецсимволов (`&`, `<`) в комментариях.

### 🛡️ Security & Sanitization (`security`)
*   **XSS Sanitization:** Эмуляция ввода вредоносного скрипта `<script>alert(1)</script>` и проверка его очистки.

### ⚡ Performance (`perf`)
*   **Large Array Filter:** Фильтрация массива из 50,000 элементов. Тест провален, если выполняется дольше 50мс.

### 🌍 I18N & Unicode (`i18n`)
*   **URL Encoding:** Проверка кодирования кириллических путей файлов (S3/Drive совместимость).

---

## 3. Как добавить новый тест

1.  Откройте `services/testSuite.ts`.
2.  Найдите нужную группу в массиве `TEST_SUITE` (или создайте новую).
3.  Добавьте объект в массив `tests`:
    ```typescript
    res.push({
        name: 'Название теста',
        description: 'Что проверяем',
        passed: condition === true, // boolean
        expected: 'Ожидаемое значение',
        received: 'Полученное значение',
        passCondition: 'Условие успеха',
        failCondition: 'Почему может упасть'
    });
    ```
4.  UI обновится автоматически.

## 4. Интеграционное тестирование

Тесты запускаются в реальном окружении браузера.
*   **Network Tests:** Используют нативный `fetch`.
*   **Performance:** Используют `performance.now()`.

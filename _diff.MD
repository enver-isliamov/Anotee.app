# 🛡️ i18n Migration Status Board

Ниже — актуальный статус миграции на `i18next` с сохранением обратной совместимости.

---

## ✅ ВЫПОЛНЕНО (Completed)

### 📦 PR‑0: Подготовка и Safety Net
*   [x] Вынести старый словарь в `services/i18n.legacy.ts`.
*   [x] Реализовать `legacyT` для fallback-поведения.
*   [x] Обновить `services/i18n.tsx` для использования Double Fallback (JSON -> Legacy).
*   [x] Добавить dev-warning в консоль при использовании legacy ключей.

### 🏗️ PR‑1: Инфраструктура
*   [x] Установить `i18next`, `react-i18next`, `i18next-browser-languagedetector`.
*   [x] Создать конфиг `services/i18n.config.ts`.
*   [x] Подключить инициализацию в App (через импорт в `i18n.tsx`).

### 📝 PR‑2: Контент (JSON Словари)
*   [x] Создать `services/locales/en.json` (100% покрытие legacy).
*   [x] Создать `services/locales/ru.json` (100% покрытие legacy).
*   [x] Создать заглушки/копии для es, ja, ko, pt.

### 🛠️ PR‑3: Контроль качества (Tooling)
*   [x] Установить `i18next-parser`.
*   [x] Создать конфиг `.i18next-parser.config.js`.
*   [x] Добавить скрипты `i18n:extract` и `i18n:check` в `package.json`.

### 🎨 Дополнительно: UI Рефакторинг (Hardcoded Strings)
*   [x] `components/Player.tsx`: Заменить жесткие строки ("Upgrade Required", "Comments locked" и др.) на ключи.
*   [x] `components/ProjectView.tsx`: Локализовать модальные окна, баннеры ограничений и кнопки.
*   [x] Добавить новые ключи в `en.json` и `ru.json`.

---

## ⏳ В ОЖИДАНИИ (Pending / Future)

### 🚀 PR‑4: Очистка (Final Polish)
*Выполнять только после подтверждения стабильной работы на проде в течение недели.*
*   [ ] Удалить `services/i18n.legacy.ts`.
*   [ ] Убрать логику fallback из `services/i18n.tsx`.
*   [ ] Удалить warning-логи из консоли.

---

## 🔍 Как протестировать текущую сборку

1.  **Проверка перевода:** Смените язык на Русский. Весь интерфейс (включая Плеер и Дашборд) должен быть переведен.
2.  **Проверка Fallback:** Если вы удалите ключ из `ru.json`, текст все равно должен отобразиться (подтянется из `i18n.legacy.ts`), а в консоли появится желтое предупреждение `[i18n] Missing key...`.
3.  **Проверка парсера:** Запустите `npm run i18n:extract` — новые ключи из кода должны автоматически попасть в JSON файлы.
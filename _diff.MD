
# План миграции локализации (i18n)

Цель: Перевести локализацию на `i18next` + JSON без поломки текущего UX.

---

### ✅ PR-1: Инфраструктура (ЗАВЕРШЕНО)
Базовая настройка движка и миграция существующих словарей.

*   [x] **Установка зависимостей:** Добавлены `i18next`, `react-i18next` в `package.json` и CDN importmap.
*   [x] **JSON Ресурсы:** Созданы файлы `services/locales/{en,ru,es,ja,ko,pt}.json`. Все старые переводы перенесены туда.
*   [x] **Конфигурация:** Создан `services/i18n.config.ts` с инициализацией и прямым импортом JSON (для совместимости с SPA без бэкенда).
*   [x] **Адаптер:** Переписан `services/i18n.tsx`. `LanguageProvider` теперь проксирует вызовы в `i18next`, сохраняя обратную совместимость с `useLanguage`.

**Итог:** Приложение работает на новом движке, но внешне ничего не изменилось.

---

### ✅ PR-2: Зачистка хардкода (ЗАВЕРШЕНО)
Убираем остатки английского текста в интерфейсе, которые раньше не были переведены.

1.  **Player.tsx:**
    *   [x] Найти строки: "Access Restricted", "Media Offline", "Fix Permissions", "S3 Connection Error" и т.д.
    *   [x] Вынести их в `en.json` и `ru.json`.
    *   [x] Заменить на вызовы `t(...)`.
2.  **ProjectView.tsx:**
    *   [x] Найти строки: "Invite feature is locked...", "Public Links are locked...", "Manage Access", "Open Org Settings".
    *   [x] Вынести и заменить.
3.  **Profile.tsx:**
    *   [x] Локализовать заголовки и кнопки S3 ("Конфигурация", "Проверить").
    *   [x] Вынести ключи в JSON.

---

### 🔮 PR-3: Оптимизация (Future)
*   [ ] Разделение `common.json` на неймспейсы (`player`, `dashboard`, `admin`).
*   [ ] Настройка скрипта для авто-проверки отсутствующих ключей (`i18next-parser`).

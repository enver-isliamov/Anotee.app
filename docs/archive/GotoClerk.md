
# ☁️ Clerk Migration Architecture (Completed)

**Статус:** ✅ **Миграция завершена**
Этот документ описывает архитектуру перехода на нативные возможности Clerk. Весь функционал внедрен и работает в продакшене.

## 🏗 Реализованная Архитектура

### 1. Профиль и Настройки
- **Компонент:** `<UserProfile />` (Clerk Native) используется для управления аккаунтом.
- **Настройки:** Язык и Тема синхронизируются через `user.unsafeMetadata.settings` (Cloud Sync).
- **Drive Token:** Получается автоматически через OAuth Connection (`api/driveToken.js`), ручная привязка удалена.

### 2. Организации (Teams)
- **Доступ:** Проверка прав в `api/_permissions.js` базируется на `orgId` из токена.
- **UI:** `<OrganizationSwitcher />` в шапке для переключения контекста.
- **Приглашения:** Используется нативный механизм приглашений Clerk.

### 3. Аутентификация
- **Guest Mode:** Полностью удален.
- **Login:** Только через `<SignIn />` (Google / Email Link).
- **Security:** `verifyUser` в `api/_auth.js` проверяет JWT и Custom Claims.

### 4. Миграция Данных (Legacy)
- В `Profile.tsx` доступна кнопка "Восстановление данных" для старых пользователей.
- Бэкенд (`api/admin.js?action=migrate`) ищет проекты по старому email и переписывает `owner_id` на новый Clerk ID.

---

## 🗑 Phase 5: Cleanup (Future Roadmap)
*Задачи по очистке кода после того, как 100% старых пользователей мигрируют.*

1.  **Удалить UI миграции:**
    *   Убрать блок `migrationStatus` и кнопку в `components/Profile.tsx`.
2.  **Удалить API миграции:**
    *   Убрать блок `if (action === 'migrate')` в `api/admin.js`.
3.  **Очистка БД:**
    *   Удалить индексы по `email` (если создавались), оставить только `owner_id` (clerk_id) и `org_id`.

---

## ✅ Чек-лист проверки (Maintenance)
1.  **Tokens:** Убедитесь, что в Clerk Dashboard > API Keys включен `Playground` или `Development` режим для локальной разработки.
2.  **Scopes:** Проверьте, что у Google Connection есть скоуп `https://www.googleapis.com/auth/drive.file`.
3.  **Webhooks:** (Опционально) Настроить Clerk Webhooks для синхронизации создания пользователей в свою БД (сейчас не требуется, используется lazy creation).

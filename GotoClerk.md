
# ☁️ Clerk Migration Plan (SmoTree)

План полного перехода на нативные возможности Clerk для отказа от кастомного кода ("костылей").

## 🚨 Предварительные требования (Critical)
- [x] В **Clerk Dashboard > Social Connections > Google** (нажать шестеренку ⚙️) включить опцию **"Use custom credentials"**.
- [x] Там же в поле **Scopes** добавить: `https://www.googleapis.com/auth/drive.file`.
    *   *Зачем:* Чтобы при входе через Google сразу выдавались права на Диск, и не нужна была отдельная кнопка "Connect Drive" с ручной обработкой токенов.

---

## 📦 Этап 1: Профиль и Настройки (UI/UX)
*Цель: Заменить `Profile.tsx` и ручное управление темой/языком.*

- [x] **UserProfile Компонент**:
    - Внедрить компонент `<UserProfile />` на страницу профиля.
    - Убрать кастомную верстку аватара и смены имени.
- [x] **Метаданные (Settings)**:
    - Переписать `LanguageProvider` и `ThemeProvider`.
    - Вместо `localStorage`, читать/писать в `user.unsafeMetadata.settings` (позволяет синхронизировать настройки между устройствами).
- [x] **Чистка**: Удалить код ручного ретрая токенов в `Profile.tsx` (Clerk SDK делает это сам, если скоупы настроены в дашборде).

## 🏢 Этап 2: Организации (Teams)
*Цель: Заменить массив `team: []` в JSON проекта на нативные Организации.*

- [x] **Включить Organizations** в Clerk Dashboard.
- [x] **UI**: Добавить `<OrganizationSwitcher />` в `AppHeader`.
- [x] **Логика создания**:
    - При создании проекта привязывать его к `orgId` текущей активной организации.
- [x] **Логика доступа (Бэкенд)**:
    - В `api/data.js`: Проверять доступ не по `project.team.includes(user)`, а по `auth.orgId`.
    - Если `orgId` совпадает, давать доступ.
- [x] **Приглашения**:
    - Удалить `api/join.js`.
    - Использовать `<OrganizationProfile />` для приглашения коллег по Email.

## 🔐 Этап 3: Аутентификация (Security)
*Цель: Удалить самописную гостевую авторизацию.*

- [x] **Отключение Guest Mode**:
    - Удалить `components/Login.tsx` (кастомную форму).
    - Использовать `<SignIn />` и `<SignUp />` для всех.
    - Для "быстрого" входа настроить "Email Link" (без пароля) в Clerk.
- [x] **API Cleanup**:
    - Удалить `api/guest.js`.
    - В `api/_auth.js` убрать проверку HMAC подписей. Оставить только `verifyToken`.

## 🔄 Этап 4: Миграция Данных
- [x] Написать скрипт (или API ручку) для массового обновления существующих проектов:
    - Перенести `ownerId` (старый) в `createdBy` (новый).
    - Присвоить проектам `orgId` (Личная организация пользователя).
    - Реализовать UI триггер в Профиле для запуска миграции.

---

## ✅ Как проверять?
1. **Ошибка консоли исчезла:** Перезагрузите страницу. Красный экран ошибки `useUser` должен исчезнуть.
2. **Mock Mode работает:** Если вы отключите API Key в `.env`, приложение должно запуститься в Mock Mode без падений.
3. **Миграция:** Зайдите в Профиль (когда авторизованы через Clerk). Внизу должна быть панель "Legacy Data Migration". Нажмите кнопку для симуляции процесса.
